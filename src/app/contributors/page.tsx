import type { Metadata } from "next";
import { ComingSoon } from "@/components/ComingSoon";

export const metadata: Metadata = { title: "Contributors" };

export default function ContributorsPage() {
  return <ComingSoon title="Contributors" />;
}
